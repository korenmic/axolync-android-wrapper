# Wrapper Families

This tree is the target neutral wrapper authority layout for the future `axolync-platform-wrapper` repo.

The current checkout still keeps the Gradle/Capacitor Android project at the repository root for compatibility. `config/wrapper-layout.json` maps the target wrapper-family paths to those active compatibility paths until the physical move is proven by builder and Android artifact parity.
